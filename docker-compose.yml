services:
  crewtracker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crewtracker-app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
